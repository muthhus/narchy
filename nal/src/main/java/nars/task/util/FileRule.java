package nars.task.util;


/**
 *
 * binds a term pattern to a URL pattern, exposing its data as NARS data
 * it can use a VFS abstraction layers to access a variety of targets
 *
 * useful for:
 *   local files
 *   general http resources
 *   ipfs
 *   web api's (JSON)
 *
 */
public class FileRule {
}
