package nars.op.sys;

/**
 * memory control-related functions:
 *  mem(load, url, #result)
 *  mem(save, url, [param], #result)
 *  mem(fade, amount) - fade active memory
 *  mem(freeze) - zero all budget
 *  mem(random) - pull items at random from index and assign random priorities, until memory is full
 */
public class mem {
}
