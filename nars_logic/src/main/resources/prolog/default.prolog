
'-->'(A, C) :- A\=C, '-->'(A,B), '-->'(B,C).
'<->'(A, B) :- A\=B, '-->'(A,B), '-->'(B,A).

/*


'==>'(A, C) :- '==>'(A,B), '==>'(B,C).

'<->'(A, B) :- '<->'(B,A).
'--'('<->'(A, B)) :- '--'('-->'(A,B)), '-->'(B,A).

'<=>'(A, B) :- '<=>'(B,A).
'<->'(A, B) :- '<=>'(A,B).

'--'('<=>'(A, B)) :- '--'('<->'(A,B)).

A :- '--'('--'(A)).
*/